import { div } from "avita"

export default function Todos() {
    return div()
        .id("todos")
        .display("flex")
        .flexDirection("column")
        .justifyContent("center")
        .alignItems("center")
}
